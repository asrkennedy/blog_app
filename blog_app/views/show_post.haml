- if @show_post.any?
  %ul
    - @show_post.each do |post|
      %li
        %h2= "#{post['title']}"
        %p= "#{post['content']}"
        %p.create= "Created on #{post['created_at']}"

%footer
  %a{href: "/"} Return to home