- if @posts.any?
  %ul
    - @posts.each do |post|
      %li
        %h2= "#{post['title']}"
        %p="#{post['content']}"
        %p= "Created at #{post['created_at']}"

%footer
  %a.btn{href: "/admin_connect"} Login